{
  "name": "@appza/schemas",
  "version": "0.0.0",
  "private": true,
  "description": "Zod schemas — the typed contract layer between Core (Laravel), the WP plug-in, the mobile app, and the plug-in admin simulator.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
